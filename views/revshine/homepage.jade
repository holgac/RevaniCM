div.container
	div.row
		div.col-md-12(ng-repeat='article in articles')
			i.pull-right
				| {{article.created | date}}
			h2
				| {{article.title}}
			p(ng-bind-html='article.content')

			div
				| {{article.creator}} @ {{article.created | time}}
